export function apiError(code, message, status = 400) { const error = new Error(message); error.code = code; error.status = status; return error; }
export function errorHandler(error, _req, res, _next) { if (error.type === 'entity.parse.failed') return res.status(400).json({ error: { code: 'MALFORMED_JSON', message: 'Request body must be valid JSON.' } }); if (error.status && error.code) return res.status(error.status).json({ error: { code: error.code, message: error.message } }); console.error(error); return res.status(500).json({ error: { code: 'INTERNAL_ERROR', message: 'An unexpected error occurred.' } }); }
